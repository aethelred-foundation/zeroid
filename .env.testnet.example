# ═══════════════════════════════════════════════════════════════
# ZeroID — TESTNET frontend config (accurate var names, matches the code)
# ═══════════════════════════════════════════════════════════════
# For a production-optimised testnet frontend, copy this file to
# `.env.production.local` and fill the addresses BEFORE `npm run build`.
# The names below are the ones the code actually reads (src/config/chains.ts,
# src/config/constants.ts). Only NEXT_PUBLIC_* vars reach the browser.

# ── Network ────────────────────────────────────────────────────
# Selects the testnet chain (id 7332) as active.
NEXT_PUBLIC_CHAIN_ENV=testnet
NEXT_PUBLIC_AETHELRED_NETWORK=testnet
NEXT_PUBLIC_ZEROID_ENV=testnet
# EVM RPC — point at a validator's PUBLIC IP (the *.aethelred.network domains are
# not deployed). This is the var chains.ts reads for the testnet chain:
NEXT_PUBLIC_AETHELRED_TESTNET_RPC_URL=http://54.165.44.130:8545

# Backend identity API (the :4003 service). REQUIRED — the dashboard and most
# screens fetch from here ("Failed to load identity" means this is unset). The
# code reads NEXT_PUBLIC_ZEROID_API_URL (constants.ts); if unset it falls back to
# the never-deployed https://api.zeroid.aethelred.network → net::ERR_NAME_NOT_RESOLVED.
# This URL is resolved by the BROWSER, so use the host reachable from the user's
# machine. `localhost` is correct only when that browser itself is running on
# the backend host. For the current shared US test VPS, use its public address
# and add the frontend origin to the backend's CORS/allowed-origin settings.
# Note: this is NOT necessarily the RPC host above — the RPC runs on a validator,
# the API on the dApps box.
NEXT_PUBLIC_ZEROID_API_URL=http://93.127.132.52:4003
# Compatibility alias read by a small number of older feature surfaces. Keep it
# identical to NEXT_PUBLIC_ZEROID_API_URL.
NEXT_PUBLIC_API_URL=http://93.127.132.52:4003

# Temporary pre-TLS testnet gate. When true, the production CSP adds only the
# exact HTTP/WS origins configured above (and for the selected RPC), never a
# broad `http:` source. Remove this after the frontend, API, and RPC use TLS.
ZEROID_ALLOW_PLAINTEXT_HTTP=true

# Next.js server-side API routes proxy a small set of proof/credential requests
# to this URL. The temporary plaintext gate below permits this exact local URL
# during public-testnet bring-up; use HTTPS and remove the gate after TLS lands.
ZEROID_BACKEND_API_URL=http://127.0.0.1:4003
ZEROID_BACKEND_FETCH_TIMEOUT_MS=5000

# ── Core identity contracts (from DeployIdentity.s.sol output) ──
# Fill these with the addresses the deploy prints. Until then they default to
# empty and those screens stay inert (the app still renders + wallet connects).
# Registration write target and paused() pre-flight target. The API verifies
# every registration against its own IDENTITY_REGISTRY_ADDRESS, so this value
# MUST be the same manifest identityRegistry the API is configured with, and
# NEXT_PUBLIC_CHAIN_ENV must be a DID network the API allows for its
# AETHELRED_CHAIN_ID (7332 -> testnet|devnet, 7331 -> mainnet) or registration
# answers 400 IDENTITY_DID_NETWORK_MISMATCH. Until the API has AETHELRED_RPC_URL
# and IDENTITY_REGISTRY_ADDRESS set, registration answers 503
# IDENTITY_REGISTRY_NOT_CONFIGURED and the wizard shows it as unavailable.
NEXT_PUBLIC_IDENTITY_REGISTRY_ADDRESS=
NEXT_PUBLIC_ZK_VERIFIER_ADDRESS=
NEXT_PUBLIC_ACCUMULATOR_REVOCATION_ADDRESS=
NEXT_PUBLIC_GOVERNANCE_MODULE_ADDRESS=
NEXT_PUBLIC_CREDENTIAL_REGISTRY_ADDRESS=
NEXT_PUBLIC_SELECTIVE_DISCLOSURE_ADDRESS=

# ── Not deployed on this chain (leave empty) ───────────────────
# AETHEL is the NATIVE coin (precisebank), not an ERC-20; there is no standalone
# TEE-attestation contract in the current suite.
NEXT_PUBLIC_AETHEL_TOKEN_ADDRESS=
NEXT_PUBLIC_TEE_ATTESTATION_ADDRESS=
NEXT_PUBLIC_BBS_PLUS_ADDRESS=
NEXT_PUBLIC_THRESHOLD_CREDENTIAL_ADDRESS=
NEXT_PUBLIC_BRIDGE_CONTRACT_ADDRESS=
NEXT_PUBLIC_REGULATORY_COMPLIANCE_ADDRESS=

# ── Cryptographic activation gates ─────────────────────────────
# Do not enable these until their ceremony artifacts and on-chain registrations
# have been reviewed and smoke-tested.
NEXT_PUBLIC_CANONICAL_VERIFY=false
NEXT_PUBLIC_AETHELRED_VKEYS={}
NEXT_PUBLIC_PQC_SIGNING=false
NEXT_PUBLIC_ZK_CIRCUIT_BASE_URL=/circuits
NEXT_PUBLIC_ZEROID_ALLOW_BROWSER_TOKEN_STORAGE=false

# ── Optional ───────────────────────────────────────────────────
# WalletConnect is NOT wired in the current wagmi config (only the injected
# connector is enabled), so no projectId is needed to connect a browser wallet.
# The API's KMS, TEE, sanctions, government-API, and regulatory variables belong
# in backend/.env; they are not needed for a base testnet render, wallet
# connection, or reads from the six core contracts.

# Enterprise identity-assurance steps in the registration wizard. Off by default,
# so testnet onboarding is just Connect Wallet → Register. Enable (and configure
# the matching backend credentials) only for a gov/enterprise deployment. These
# are read at build time, so set them BEFORE `npm run build`.
NEXT_PUBLIC_UAE_PASS_ENABLED=false
NEXT_PUBLIC_TEE_BIOMETRIC_ENABLED=false
